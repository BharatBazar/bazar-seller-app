import * as React from 'react';
import { View } from 'react-native';
import Page from './AudioPlayer';

interface TextPhotoAudioInputComponentProps {}

const TextPhotoAudioInputComponent: React.FunctionComponent<TextPhotoAudioInputComponentProps> = () => {
    return <View />;
};

export default TextPhotoAudioInputComponent;
